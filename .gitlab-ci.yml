variables:
  GIT_DEPTH: 10

stages:
  - PEP8
  - fetch
  - data
  - interface
  - threholds
  
  
  
  
#PEP8_Comformity:
#  stage: PEP8
#  image: CREATE A DOCKER IMAGE TO RUN ON SERVER
#  script:
#    - "pytest --pep8 --disable-warnings"
#  tags:
#    - jarvis
  
Fetch_data_and_models:
  stage: fetch
  cache:
    key: default
    paths:
        - prod_data/test/
        - prod_models/candidate/
  script:
    - "source /home/pi/Documents/esa/venv/bin/activate"
    - "dvc remote modify --local nielspi password projectesa"
    - "dvc pull"
  tags:
    - audio

Data_validation:
  stage: data
  script:
    - "echo Testing data schema and values"
  tags:
    - audio
    
    
Interface_validation: 
  stage: interface
  script:
    - "echo Testing DNNs interface specification"
  tags:
    - audio


Threshold_validation: 
  stage: threholds
  cache:
    key: default
    paths:
        - prod_data/test/
        - prod_models/candidate/
  script: 
    - "echo Testing DNN metrics against threshold values"
    - "source /home/pi/Documents/esa/venv/bin/activate"
#    - "unzip /home/gitlab-runner/cache/gitlab/st-c2019/esa/audio-sentiment-analysis/default/cache.zip"
    - "python tests/compute_models_performance.py"
  tags:
    - audio
